//use the rest operator with function parameters
//it allows us to create a function with which we can take varying number of arguments
//the rest operator is three dots(...args)
function varArgs(...args) 
{
    return a+b+c;
}
varArgs(3,4,5)
